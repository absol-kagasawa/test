import {StyleSheet} from 'react-native';
import {navBarHeight} from '../../../../platform/metrics';

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'white',
  },
  webview: {
    flex: 1,
  },
});
